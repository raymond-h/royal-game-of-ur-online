{
  "scripts": {
    "install": "cd service && npm install",
    "http-server": "http-server -p $PORT ./static/"
  },
  "cacheDirectories": [
    "node_modules",
    "service/node_modules"
  ],
  "engines": {
    "node": ">=7.6.0 <8.0.0"
  },
  "dependencies": {
    "http-server": "^0.10.0"
  }
}
