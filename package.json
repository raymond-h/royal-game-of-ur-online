{
  "scripts": {
    "install": "cd backend/ && npm install && cd ../service && npm install"
  },
  "dependencies": {
    "deepstream.io": "^2.2.2"
  },
  "engines": {
  	"node": ">=7.6.0 <8.0.0"
  }
}
