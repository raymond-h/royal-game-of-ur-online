{
  "scripts": {
    "install": "cd backend/ && npm install && cd ../service && npm install"
  },
  "cacheDirectories": ["node_modules", "backend/node_modules", "service/node_modules"],
  "engines": {
  	"node": ">=7.6.0 <8.0.0"
  }
}
